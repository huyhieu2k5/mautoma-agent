/**
 * computer-control — Lightweight stub for local type-check.
 */

export function createComputerControl(): { ready: true } {
  return { ready: true };
}

export function createWorkflows(): { ready: true } {
  return { ready: true };
}
